import React from "react";
import ChatList from "../components/chats/ChatList";

const Home = () => {
    return (
        <div className="w-full h-full">
            <ChatList></ChatList>
        </div>
    );
};

export default Home;
